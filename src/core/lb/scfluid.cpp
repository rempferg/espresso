SCFluid();
void integrate()
{
  //lb[0,1].calc_modes()
  //rho = calc_rho(lb1, lb2); u = calc_u(lb1, lb2)
  //lb[0,1].relax(rho, u)
  //lb[0,1].thermalize()
  //lb[0,1].apply_force()
  //lb[0,1].calc_n_from_n_push()
}
std::vector<LBFluid> lb;
